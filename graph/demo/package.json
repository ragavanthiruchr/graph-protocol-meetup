{
  "name": "demo",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ test/demo",
    "create-local": "graph create --node http://localhost:8020/ test/demo",
    "remove-local": "graph remove --node http://localhost:8020/ test/demo",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 test/demo",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.37.1",
    "@graphprotocol/graph-ts": "0.28.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
